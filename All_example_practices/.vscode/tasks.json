{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build each cpp to obj/<name>",
      "type": "shell",
      "command": "bash",
      "args": [
        "-c",
        "mkdir -p obj && for f in src/*.cpp; do name=$(basename \"$f\" .cpp); g++ \"$f\" -Iinclude -o \"obj/$name\"; done"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": ["$gcc"]
    }
  ]
}