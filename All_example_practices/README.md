#Git clone the repo
#git add the pracice .cpp file in src and respective header in the include directory
#Do build the file and the respective obj file will be created in the obj directory
#git.sh file to pusht he changes tot eh git repo automatically
